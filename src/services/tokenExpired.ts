/**
 * src/services/tokenExpired.ts
 * FASE 1 — Eliminado conflicto de nombres duplicados.
 * La lógica vive en utils/decode.ts y services/api.ts.
 * Este archivo se mantiene por compatibilidad con imports existentes.
 */
export { Token, decodeToken, decodeTokenPublic } from '../utils/decode'
